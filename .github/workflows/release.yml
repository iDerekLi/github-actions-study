name: Release1
on:
  push:
    tags: ["v*"]
jobs:
  masterb:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: push
        run: |
          git add .
          git commit -m "ci:push"
          git push main

