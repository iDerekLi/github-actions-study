name: Release1
on:
  push:
    tags: ["v*"]
jobs:
  masterb:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: push
        run: |
          git config --global user.email "lideyu888@163.com"
          git config --global user.name "DerekLi"
          git checkout -b dev
          git add .
          git commit -m "ci:push"
          git push main

