name: Release1
on:
  push:
    tags: ["v*"]
jobs:
  masterb:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - run: |
          git checkout main
          git merge dev
          git pugh origin main

