name: Release1
on:
  push:
    tags: ["v*"]
jobs:
  masterb:
    runs-on: ubuntu-latest
    steps:
      - run: git checkout main
      - run: git merge dev
      - name: push main
        run: git push origin main

